MODELO VISTA CONTROLADOR
M = MODELO (BASE DE DATOS)
V = VISTA (PANTALLAS)
C = CONTROLADOR

MODELO VISTA TEMPLATE
M = MODELO (BASE DE DATOS)
V = (FUNCIONES)
T = (PANTALLAS)


instalar librerias con pip para crear entorno virtual

asgiref==3.2.7

Django==3.0.4

django-crum==0.7.5

django-widget-tweaks==1.4.8

freeze==1.0.10

Pillow==7.1.1

pytz==2019.3

six==1.14.0

sqlparse==0.3.1
